#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	int num1,num2;
	scanf("%d%d", &num1, &num2);
	printf("Hello %d C++ %d\n",num1,num2);
	scanf("%d",&num1);
	printf("%d",num1);
	return 0;
}
